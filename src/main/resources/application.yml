server:
  port: 8900

spring:
  datasource:
    url: jdbc:mysql://192.168.31.166:3306/min-mall?autoReconnect=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapping/*.xml

app:
  cors:
    allowOrigin: ${APP_CORS_ORIGIN:http://192.168.31.166:9527,http://192.168.31.166:8900}
    allowMethods: POST, GET, PUT, OPTIONS, DELETE
    allowHeaders: x-prototype-version,x-requested-with, authorization, Content-Type, Authorization, credential, X-XSRF-TOKEN, x-auth-token
    allowCredentials: true

